from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy()
"""
The SQLAlchemy class is used to initialize the Flask-SQLAlchemy extension.
It creates the SQLAlchemy object and initializes it with the Flask application.
This object can then be used to interact with the database.
"""
